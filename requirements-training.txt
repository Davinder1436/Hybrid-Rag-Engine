# Training & RAG specific dependencies (builds on requirements.txt)
# Install with: pip install -r requirements.txt -r requirements-training.txt

# Config & CLI
hydra-core>=1.3.0,<2.0.0
omegaconf>=2.3.0,<3.0.0

# Additional training utilities
peft>=0.4.0,<1.0.0  # Parameter-efficient fine-tuning
tensorboard>=2.14.0

# Testing
pytest>=7.4.0,<8.0.0
pytest-cov>=4.1.0,<5.0.0
pytest-xdist>=3.3.0  # For parallel testing

# Documentation
sphinx>=7.0.0
sphinx-rtd-theme>=1.3.0
